### 💡 Conceito

**JavaScript (JS)** é uma linguagem de programação essencial para o desenvolvimento web.  
Ela dá **interatividade e dinamismo** às páginas, permitindo manipular elementos do HTML e reagir a ações do usuário.

### 🧠 Lembretes

- É **interpretada** (não precisa compilar).
    
- Roda no **navegador** e também no **servidor** (com Node.js).
    
- Usa **tipagem dinâmica** (as variáveis mudam de tipo automaticamente).
    
- O **case-sensitive** é importante: `nome` ≠ `Nome`.
    

---

### 🧩 Exemplos Práticos

``// Exibir uma mensagem console.log("Olá, mundo!");  // Declarar variáveis let nome = "Lia"; const idade = 22;  // Condicional simples if (idade >= 18) {   console.log(`${nome} é maior de idade`); }``

---

### 📚 Dicas de Estudo

- Usa o **console do navegador (F12)** pra testar códigos simples.
    
- Cria mini-projetos, como uma calculadora ou contador de cliques.
    
- Aprende a diferenciar `let`, `const` e `var`.
    
- Explora o site **MDN Web Docs**, a melhor fonte de referência em JS.