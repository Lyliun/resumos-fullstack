### 💡 Conceito

**Git** é um sistema de controle de versão que registra o histórico das alterações em um projeto.  
Ele permite trabalhar de forma colaborativa, testando novas ideias sem perder versões anteriores.  
Já o **GitHub** é uma plataforma online que hospeda repositórios Git e facilita o trabalho em equipe.

### 🧠 Lembretes

- **Git** é local (funciona no teu computador).
    
- **GitHub** é remoto (serve pra armazenar, compartilhar e colaborar).
    
- Cada projeto controlado pelo Git é chamado de **repositório**.
    
- As mudanças são salvas através de **commits**.
    
- Trabalhos em paralelo são feitos em **branches** (ramificações).
    

---

### 🧩 Exemplos Práticos

`# Iniciar um repositório git init  # Adicionar arquivos git add .  # Criar um commit git commit -m "primeiro commit"  # Vincular ao GitHub git remote add origin https://github.com/usuario/repositorio.git  # Enviar alterações git push -u origin main`

---

### 📚 Dicas de Estudo

- Cria um repositório de testes no GitHub pra praticar.
    
- Usa commits com mensagens claras e objetivas.
    
- Lembra: errar faz parte — o Git guarda tudo, então sem medo!
    
- Explora comandos como `git status`, `git log` e `git diff`.